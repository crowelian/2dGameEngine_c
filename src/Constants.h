#ifndef CONSTANTS_H
#define CONSTANTS_H
// Just include once this header file... 
// before and endif is for that

const unsigned int WINDOW_WIDTH = 800;
const unsigned int WINDOW_HEIGHT = 600;

const unsigned int FPS = 60; // target fps
const unsigned int FRAME_TARGET_TIME = 1000 / FPS; //how long it takes a frame to render

#endif